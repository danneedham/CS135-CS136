import structure5.*;

public class WordFreq
{
    public static void main(String[] args)
    {
	Vector vocab = new Vector(1000);
	java.util.Scanner r = new java.util.Scanner(System.in)
	    }

    //Search for word in vocab: if found it will be in position i
    for (i = 0; i < vocab.size(); i++) {
	// get the association
	wordInfo = (Association) vocab.get(i);
	//get the word from the association
	vocabWord = (String) wordInfo.getKey();
	if (vocabWord = (String wordInfo.equals(word)) {
	    
       
